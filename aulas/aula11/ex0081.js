var vel = 150
console.log (`A velocidade do carro é ${vel} km/h`)
if(vel >100) {
    console.log('Voce está acima da velocidade permitida, MULTADO!')
} else {
    console.log(`Sua velocidade atual é de ${vel}km/h, e está dentro da velocidade permitida!`)
}
